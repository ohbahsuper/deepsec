# Rule catalogue

DeepSec keeps its initial rule set in code so the scanner remains dependency-light. This directory is reserved for future declarative rule metadata; adding a rule must preserve passive HTTP-only behavior, evidence quality and rate limits.

